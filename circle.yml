dependencies:
  override:
    - npm install

test:
  override:
    - npm test

notify:
  webhooks:
    - url: http://146.185.167.197:3000/hook

deployment:
  production:
    branch: master
    commands:
      - ssh pigeon@146.185.167.197 '(cd /home/pigeon/apps/webhook-publisher && ggpur) && pm2 restart pm2.json'
